from setuptools import setup, find_packages

setup(
    name='oop_healthprofessional',
    version='0.1',
    packages=find_packages(),
    install_requires=[]  # Add any dependencies here (e.g., ['pandas'])
)